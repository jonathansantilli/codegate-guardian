// Cross-cutting utilities usable by any layer. Must remain free of
// framework / I/O dependencies so the domain can import from here.
//
// Phase 0: empty. Add Result<Ok, Err>, uuid helper, etc., as needed.
export {};
