// Domain layer entry point. Pure TypeScript only — no framework, no I/O,
// no Date.now(), no crypto.randomUUID(). Clock and IdGenerator arrive via
// ports defined in src/application/ports/.
//
// Phase 0: empty. Real content lands in Phase 2 (domain extraction).
export {};
