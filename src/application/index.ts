// Application layer entry point. Owns ports (src/application/ports/) and
// use cases (src/application/use-cases/). Orchestrates the domain.
// Imports: domain + own ports. Does NOT import infrastructure or frameworks.
//
// Phase 0: empty. Port interfaces land in Phases 3a–11.
export {};
